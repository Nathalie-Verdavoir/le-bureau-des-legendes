<form class="delete  btn-edit" method="post" action="{{ path('agents_delete', {'id': agent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agent.id) }}">
    <button class="btn btn-edit">{{ button_label|default('Supprimer') }}</button>
</form>
