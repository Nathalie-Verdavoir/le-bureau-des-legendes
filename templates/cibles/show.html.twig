{% extends 'welcome.html.twig' %}

{% block title %}Cibles{% endblock %}

{% block main %}
    <h1>Cibles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cible.id|e }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cible.nom|e }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ cible.prenom|e }}</td>
            </tr>
            <tr>
                <th>Date de naissance</th>
                <td>{{ cible.dateDeNaissance|e ? cible.dateDeNaissance|e|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn" href="{{ path('cibles_index', {'page': 1}) }}">Retour</a>

    <a type="button" class="btn" href="{{ path('cibles_edit', {'id': cible.id}) }}">edit</a>

    {{ include('cibles/_delete_form.html.twig') }}
{% endblock %}
