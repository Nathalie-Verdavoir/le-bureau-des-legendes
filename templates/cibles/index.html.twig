{% extends 'base.html.twig' %}

{% block title %}Cibles index{% endblock %}

{% block body %}
    <h1>Cibles index</h1><a type="button" class="btn btn-primary" href="/">Accueil</a>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Date_de_naissance</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cible in cibles %}
            <tr>
                <td>{{ cible.id }}</td>
                <td>{{ cible.nom }}</td>
                <td>{{ cible.prenom }}</td>
                <td>{{ cible.dateDeNaissance ? cible.dateDeNaissance|date('Y-m-d') : '' }}</td>
                <td>
                    <a type="button" class="btn btn-primary" href="{{ path('cibles_show', {'id': cible.id}) }}">show</a>
                    <a type="button" class="btn btn-primary" href="{{ path('cibles_edit', {'id': cible.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a type="button" class="btn btn-primary" href="{{ path('cibles_new') }}">Create new</a>
{% endblock %}
