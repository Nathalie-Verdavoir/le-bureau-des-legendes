{% extends 'welcome.html.twig' %}

{% block title %}NomDeCode{% endblock %}

{% block main %}
    <h1>NomDeCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nom_de_code.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ nom_de_code.code }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn" href="{{ path('nom_de_code_index', {'page': 1}) }}">Retour</a>

    <a type="button" class="btn" href="{{ path('nom_de_code_edit', {'id': nom_de_code.id}) }}">Modifier</a>

    {{ include('nom_de_code/_delete_form.html.twig') }}
{% endblock %}
