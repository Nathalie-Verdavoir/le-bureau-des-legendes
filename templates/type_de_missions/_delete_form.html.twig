<form method="post" action="{{ path('type_de_missions_delete', {'id': type_de_mission.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_de_mission.id) }}">
    <button class="btn">Delete</button>
</form>
