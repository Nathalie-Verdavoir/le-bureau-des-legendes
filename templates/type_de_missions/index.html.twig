{% extends 'base.html.twig' %}

{% block title %}TypeDeMissions index{% endblock %}

{% block body %}
    <h1>TypeDeMissions index</h1><a type="button" class="btn btn-primary" href="/">Accueil</a>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_de_mission in type_de_missions %}
            <tr>
                <td>{{ type_de_mission.id }}</td>
                <td>{{ type_de_mission.nom }}</td>
                <td>
                    <a type="button" class="btn btn-primary" href="{{ path('type_de_missions_show', {'id': type_de_mission.id}) }}">show</a>
                    <a type="button" class="btn btn-primary" href="{{ path('type_de_missions_edit', {'id': type_de_mission.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a type="button" class="btn btn-primary" href="{{ path('type_de_missions_new') }}">Create new</a>
{% endblock %}
