{% extends 'base.html.twig' %}

{% block title %}Specialites{% endblock %}

{% block body %}
    <h1>Specialites</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ specialite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn btn-primary" href="{{ path('specialites_index') }}">Retour</a>

    <a type="button" class="btn btn-primary" href="{{ path('specialites_edit', {'id': specialite.id}) }}">edit</a>

    {{ include('specialites/_delete_form.html.twig') }}
{% endblock %}
