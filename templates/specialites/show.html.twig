{% extends 'welcome.html.twig' %}

{% block title %}Specialites{% endblock %}

{% block main %}
    <h1>Specialites</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ specialite.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn" href="{{ path('specialites_index', {'page': 1}) }}">Retour</a>

    <a type="button" class="btn" href="{{ path('specialites_edit', {'id': specialite.id}) }}">Modifier</a>

    {{ include('specialites/_delete_form.html.twig') }}
{% endblock %}
