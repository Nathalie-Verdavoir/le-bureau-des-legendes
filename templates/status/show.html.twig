{% extends 'welcome.html.twig' %}

{% block title %}Status{% endblock %}

{% block main %}
    <h1>Status</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status.id|e }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ status.etat|e }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn" href="{{ path('status_index', {'page': 1}) }}">Retour</a>

    <a type="button" class="btn" href="{{ path('status_edit', {'id': status.id}) }}">edit</a>

    {{ include('status/_delete_form.html.twig') }}
{% endblock %}
