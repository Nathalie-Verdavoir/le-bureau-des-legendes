{% extends 'welcome.html.twig' %}

{% block title %}Contacts{% endblock %}

{% block main %}
    <h1>Contacts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id|e }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ contact.nom|e }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ contact.prenom|e }}</td>
            </tr>
            <tr>
                <th>Date de naissance</th>
                <td>{{ contact.dateDeNaissance ? contact.dateDeNaissance|e|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a type="button" class="btn" href="{{ path('contacts_index', {'page': 1}) }}">Retour</a>

    <a type="button" class="btn" href="{{ path('contacts_edit', {'id': contact.id}) }}">edit</a>

    {{ include('contacts/_delete_form.html.twig') }}
{% endblock %}
